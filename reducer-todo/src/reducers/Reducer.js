import React from 'react';

const initialState = {
    item: "Learn about reducers",
    completed: false,
    id: 3892987589
};

const Reducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}


export default Reducer;